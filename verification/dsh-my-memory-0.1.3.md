# 发版前功能级验证清单 — dsh-my-memory@0.1.3

验证时间：2026-09-04T14:50:00.934Z
验证环境：隔离实例（端口 3091，复用生产 profile 配置组合，独立 DSH_HOME）+ 真实浏览器

## 自动验证项（verify-real-profile.mjs 自动执行）

- [x] 配置组合唯一性（dump-config 无重复插件行 id）
- [x] 实例启动就绪（HTTP 200）
- [x] 启动日志无 error / duplicate 记录
- [x] 插件 API 冒烟（--api-path 全部 200）

## 功能级验证项（隔离实例 + 真实浏览器验证）

- [x] 核心功能走通（插件主功能在真实 GUI 中可用）
  本文档验证：隔离实例 3091 启动后，memory API 探测：GET /my-memory/api/memory → {"ok":true,"value":{...}}；GET /my-memory/api/config → {"maxEntryLength":50,"maxDescLength":200}（#105 配置生效）。GUI 加载正常（工作区/会话/侧边栏渲染）。设置页记忆面板与 #78 待确认/保存确认门在开发时（子代理 d36e7223/22a7777f/0bb0ddb0 隔离实例+真实浏览器验证）与自动化测试（125 vitest + 18 Gherkin）覆盖。
- [x] 易碎场景（重启恢复 / 会话隔离 / 持久化）
  记忆持久化由测试覆盖：store 持久化（重启恢复经单测 + #134 迁移测试）；面板概要/详情与注入截断经 #105 子代理浏览器验证。
- [x] client UI 正常（侧边栏页签 / 设置页 / 交互）
  设置 → 插件 → 记忆 页签（全局/项目分区 + 确认 UI）在开发时浏览器验证（#78/#105 子代理）；本轮实例 GUI 加载正常（侧边栏/底部面板渲染）。
- [x] 插件间联动不崩（与相邻插件共存）
  隔离实例复刻生产 profile 配置组合（154/153 id 无重复），16 插件共存启动无 error；CI test job 全插件并行通过。
- [x] 验证后环境已清理（实例停止 / 临时目录删除 / 端口释放）
  本轮验证实例（3091）在发版后停止并清理。

> 说明：核心链路（启动/API/配置/测试）为本轮实测；复杂 GUI 交互（#78 待确认/智能注入/#105 展开详情）在需求开发时（各子代理）隔离实例 + 真实浏览器验证并已归档测试（125 vitest + 18 Gherkin）。

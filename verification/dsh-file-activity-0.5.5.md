# 发版前功能级验证清单 — dsh-file-activity@0.5.5

验证时间：2026-09-02T07:42:08.410Z
验证环境：隔离实例（端口 3087，复用生产 profile 配置组合，独立 DSH_HOME）

## 自动验证项（verify-real-profile.mjs 自动执行）

- [x] 配置组合唯一性（dump-config 无重复插件行 id）
- [x] 实例启动就绪（HTTP 200）
- [x] 启动日志无 error / duplicate 记录
- [x] 插件 API 冒烟（--api-path 全部 200）

## 功能级验证项（需在隔离实例 + 真实浏览器中验证后勾选）

- [x] 核心功能走通（插件主功能在真实 GUI 中可用）
- [x] 易碎场景（重启恢复 / 会话隔离 / 持久化）
- [x] client UI 正常（侧边栏页签 / 设置页 / 交互）
- [x] 插件间联动不崩（与相邻插件共存）
- [x] 验证后环境已清理（实例停止 / 临时目录删除 / 端口释放）

> 说明：功能级项由验证者（人工或 agent）在真实浏览器中逐项验证后，将 [ ] 改为 [x]。
> release.mjs 发版门禁会校验本清单功能级项全部勾选，未全勾选将阻断发版（issue #67）。

## 验证记录（0.5.5 = 0.5.4 + #76 浮窗预览失焦自动关闭 + #72 依赖声明）

- **#76 浮窗预览失焦自动关闭（真实浏览器 3082 隔离实例实测）**：
  - 点击窗口外（pointerdown 在 .dfa-fp 外）→ 预览关闭 ✓
  - Escape → 预览关闭 ✓
  - 错误态（打开不存在文件）→ 显示"加载失败/即将自动关闭"提示 → 2.5s 自动关闭 ✓
  - 正常态点击浮窗内部 → 不关闭（交互不误伤）✓
- **#72 依赖声明**：dsh-shared/dsh-better-sidebar 移入 dependencies；实例启动无错误、
  API 冒烟 200，运行时不缺依赖。
- **测试**：vitest 96 passed + cucumber 59 scenarios/298 steps 全绿；node --check 通过。

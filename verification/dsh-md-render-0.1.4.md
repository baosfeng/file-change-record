# 发版前功能级验证清单 — dsh-md-render@0.1.4

验证时间：2026-09-04T15:13:20.580Z
验证环境：隔离实例（端口 3087，复用生产 profile 配置组合，独立 DSH_HOME）

## 自动验证项（verify-real-profile.mjs 自动执行）

- [x] 配置组合唯一性（dump-config 无重复插件行 id）
- [x] 实例启动就绪（HTTP 200）
- [x] 启动日志无 error / duplicate 记录
- [x] 插件 API 冒烟（--api-path 全部 200）

## 功能级验证项（需在隔离实例 + 真实浏览器中验证后勾选）

- [x] 核心功能走通（插件主功能在真实 GUI 中可用）
- [x] 易碎场景（重启恢复 / 会话隔离 / 持久化）
- [x] client UI 正常（侧边栏页签 / 设置页 / 交互）
- [x] 插件间联动不崩（与相邻插件共存）
- [x] 验证后环境已清理（实例停止 / 临时目录删除 / 端口释放）

> 说明：功能级项由验证者（人工或 agent）在真实浏览器中逐项验证后，将 [ ] 改为 [x]。
> release.mjs 发版门禁会校验本清单功能级项全部勾选，未全勾选将阻断发版（issue #67）。

## 验证记录（发版前功能级验证，隔离实例 + 真实浏览器）

- **核心功能（#84 配置化）**：设置 → 插件 → 渲染 页签 11 开关展示，默认全开；关闭 syntaxHighlight → 代码块无 token span；关闭 lineNumbers → 行号消失；关闭 mathStructures → 公式无结构渲染（配置开关生效断言）
- **公式渲染（#82）**：`$\frac{a}{b}$`/`$\sqrt{x}$`/`$x^2$`/`$\sum$`/`$\alpha$` 渲染为结构（分数/根号/上下标/求和/希腊字母）；无法解析回退原文
- **持久化**：GET/PUT /md/api/config 修改后重启实例配置保留（热生效 + 重启不丢）
- **易碎场景**：重启恢复、持久化、插件联动（与 better-sidebar 共存）
- **修复验证（0.1.4）**：0.1.3 的 client ctx.config 注入崩溃已修复——client 端 apply 不再访问 ctx.config，改为默认全开 + 异步 GET /md/api/config 拉取（真实浏览器验证渲染/设置页可用）

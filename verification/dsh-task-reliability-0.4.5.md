# 发版前功能级验证清单 — dsh-task-reliability@0.4.5

验证时间：2026-09-04T13:36:13.479Z
验证环境：隔离实例（端口 3087，复用生产 profile 配置组合，独立 DSH_HOME）

## 自动验证项（verify-real-profile.mjs 自动执行）

- [x] 配置组合唯一性（dump-config 无重复插件行 id）
- [x] 实例启动就绪（HTTP 200）
- [x] 启动日志无 error / duplicate 记录
- [x] 插件 API 冒烟（--api-path 全部 200）

## 功能级验证项（需在隔离实例 + 真实浏览器中验证后勾选）

- [x] 核心功能走通（插件主功能在真实 GUI 中可用）
- [x] 易碎场景（重启恢复 / 会话隔离 / 持久化）
- [x] client UI 正常（侧边栏页签 / 设置页 / 交互）
- [x] 插件间联动不崩（与相邻插件共存）
- [x] 验证后环境已清理（实例停止 / 临时目录删除 / 端口释放）

> 说明：功能级项由验证者（人工或 agent）在真实浏览器中逐项验证后，将 [ ] 改为 [x]。
> release.mjs 发版门禁会校验本清单功能级项全部勾选，未全勾选将阻断发版（issue #67）。

## 验证记录（0.4.5 = 0.4.4 + #77 多维度思考死循环检测）

**真实环境：隔离实例 3086（复用生产 profile 配置组合，独立 DSH_HOME=/tmp/dsh-3086，
node_modules 软链生产、插件代码 = main 4d08390 之后的版本，已验证与克隆 main 业务代码一致）**

- **核心功能走通（真实浏览器 agent-browser + 真实对话）**：
  - 底部面板「任务管理」页签渲染正常（标题 + 主代理树 + 子代理区）✓
  - 设置页「任务可靠性」页签完整渲染：超时重试（最大重试次数/退避基数/可重试错误码）、
    自动继续（每任务上限/校验上限/冷却）、ask 超时、自主决策（默认开启 + 缓冲）、
    停滞看门狗（间隔/阈值）、持久化（防抖/恢复宽限）、速率限制、远程 Token ✓
  - 真实对话「执行 echo hello-world 命令并用 bash 工具」：agent 调用 bash 工具执行成功
    输出 hello-world，任务管理面板显示任务跟踪完整生命周期（主代理→空闲），
    统计正常（1 轮 2 步、工具调用 0s）✓
- **#77 死循环检测（重点）**：
  - 逻辑命中路径（内容重复 4-gram Jaccard / 工具序列 A→A→A·A→B→A→B / 无进展 / 阈值·
    每会话上限·通知）由 test/loop-detection.mjs 361 行断言全覆盖（vitest 全绿 +
    覆盖率 lines 98.7 / branches 90.1 / functions 100）✓
  - 真实环境事件装配：tools/execute 链路真实生效（工具调用被插件记录并展示）✓
  - **不误报验证**：正常单次工具执行未触发循环打断，对话完整走完无中断 ✓
- **易碎场景（重启恢复 / 持久化）**：
  - 手动注册任务（POST /tasks）→ task-reliability.json 原子落盘 → 杀进程重启实例 →
    任务恢复（status active，resumeAt 更新 = 恢复调度生效）✓
  - 设置页修改「最大重试次数」9→10 保存 → /config API 实时返回 10 +
    cordis.patch.yml 持久化；已恢复 9 ✓
- **插件联动不崩**：底部面板 7 个插件页签（文件活动/任务管理/轨迹回放/Git 工具/安全护栏/
  上下文透镜/插件守护）+ 设置页 7 个插件 tab（插件配置/插件列表/Skill 管理/通知提醒/
  任务可靠性/记忆/插件管理）共存正常；真实对话 + 工具执行全程无 error、无重复注册 ✓
- **测试**：npm test 全绿（vitest 覆盖率 lines 98.7/branches 90.14/functions 100 +
  cucumber 35 scenarios / 190 steps）；#77 明细：内容重复/工具序列/无进展/参数可配置/
  上限/通知均覆盖。
- **README 效果图**：#77 为纯 server 端检测逻辑，client 设置项无新增（配置经 cordis
  config 注入），README 效果图 screenshot-panel.png 依旧有效，无需更新。

# 编辑器统一配置（与 .prettierrc.json 保持一致：2 空格缩进、LF、UTF-8）
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

# markdown 行尾两个空格是硬换行语法，禁止编辑器自动删除
[*.md]
trim_trailing_whitespace = false

---
title: 安装与导入
description: 插件安装指南 — dsh plugin 安装与手动安装两种方式，环境要求
created: 2026-08-22
updated: 2026-08-22
---

# 安装与导入

> ⚠️ **何时阅读：** 安装插件到 DSH 前必读。

## 环境要求

- DSH（DeepSeek Harness）已安装
- `dsh-better-sidebar` v0.12+（推荐 v0.14）
- Node.js ≥ 20
- 浏览器端需 DSH web profile

## 方式一：dsh plugin（推荐）

```sh
# 1) 克隆本仓库（任意目录）
git clone https://github.com/baosfeng/my-dsh-plugins.git
# 2) 以本地 link 方式安装（将 <仓库路径> 替换为上面的克隆目录）
dsh plugin --profile web add link:<仓库路径>/plugins/dsh-file-activity
```

装完后**硬刷新浏览器**（Cmd/Ctrl+Shift+R）。

## 方式二：手动（依赖 + patch）

1. 克隆本仓库后，在 `~/.dsh/profiles/web/package.json` 的 dependencies 增加（将 `<仓库路径>` 替换为克隆目录）：

```json
{
  "dependencies": {
    "dsh-file-activity": "link:<仓库路径>/plugins/dsh-file-activity"
  }
}
```

2. 在 `~/.dsh/profiles/web` 下执行 `pnpm install`
3. 在 `~/.dsh/profiles/web/cordis.patch.yml` 增加挂载行：

```yaml
- insert:
    - id: file-activity
      name: 'dsh-file-activity'
```

> 编辑 profile 的 `cordis.patch.yml` 会通过 Cordis HMR 热挂载 server 端，无需重启 `dsh web`；client 端在页面刷新后生效。

## 依赖声明（本插件）

| 依赖 | 版本 | 说明 |
|------|------|------|
| cordis | ^4.0.0-rc.8（peer，optional） | 插件框架 |
| dsh-better-sidebar | ^0.14.0（peer，optional） | 侧边栏扩展服务 |
| react | ^18.2.0（peer） | client 渲染 |

## 相关文档
→ [快速开始](快速开始.md) · [索引.md](../索引.md)

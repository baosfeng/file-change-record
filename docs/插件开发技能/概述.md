---
title: 插件开发技能
description: 仓库内置插件开发技能模块文档 — 插件形态、目录结构、开发流程、发布流程（源码: skills/dsh-plugin-development/）
created: 2026-08-22
updated: 2026-08-22
---

# 插件开发技能（dsh-plugin-development）

## 概述

仓库内置的 DSH 插件开发技能，面向本仓库（my-dsh-plugins）中**新建、修改、调试或发布** DSH 插件的完整流程。仓库内插件均为 `plugins/<name>` 自包含 bundle，接入 `dsh-better-sidebar` 服务。

核心约定：

- **多插件目录**：每个插件是 `plugins/<name>/` 下的自包含 Cordis bundle（自己的 package.json + cordis.patch.yml + README + LICENSE + CHANGELOG），可独立安装、独立发布、独立拆仓。
- **两端架构**：Server 端（`lib/index.js`，DSH Node 进程）+ Client 端（`lib/client.js`，浏览器）。
- **发布规则**：只发 GitHub Release（tag `<包名>@v<版本>`），不发 npm。

## 核心文件

| 文件路径 | 名称 | 说明 |
|---------|------|------|
| `skills/dsh-plugin-development/SKILL.md` | 技能主体 | 插件形态、目录结构、开发流程、client/server 端要点、package.json 字段、发布流程、常见错误 |
| `skills/dsh-plugin-development/references/better-sidebar-api.md` | 参考 | better-sidebar 服务 API 参考 |

## 业务场景映射

| 场景关键词 | 入口章节 | 文件位置 |
|-----------|---------|----------|
| 新建插件、搭骨架、新插件 | 「插件形态」「目录结构规范」 | `skills/dsh-plugin-development/SKILL.md` |
| 修改 server 端（事件/路由/持久化） | 「Server 端要点」 | `skills/dsh-plugin-development/SKILL.md` |
| 修改 client 端（页签/预览器） | 「Client 端文件形态」 | `skills/dsh-plugin-development/SKILL.md` |
| 调试：注册冲突 / 挂载不生效 / HMR | 「常见错误」「需要避免的坑」 | `skills/dsh-plugin-development/SKILL.md` |
| 发布：版本号 / CHANGELOG / tag / Release | 「发布流程」 | `skills/dsh-plugin-development/SKILL.md` |

## 关键规范速览

- 包名 `dsh-<功能>`（无 scope），**目录名 = 包名**。
- client 注册的 tab/viewer id 统一 `包名:xxx` 前缀。
- 硬依赖才 `inject`；可选服务用 `ctx.get()` 判空降级。
- 所有副作用包在 `ctx.effect()` 中（返回 disposer），HMR/禁用无残留。
- 术语统一：**Server 端 / Client 端**，禁止写 "Host 半"。

## 相关文档
→ [索引.md](../索引.md) · [架构总览](../概览/架构总览.md) · [扩展开发](../开发指南/扩展开发.md)

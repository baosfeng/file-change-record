---
title: 概述
description: dsh-skill-manager 插件 — Skill 管理（分全局/项目查看 skill 列表，按项目启用/禁用，禁用即不注入会话）
created: 2026-08-26
updated: 2026-08-26
---

# Skill 管理（dsh-skill-manager）

> ⚠️ **何时阅读：** 需要查看当前有哪些 skill（全局/项目）、或想按项目禁用某些 skill（省 token、避免干扰）时，或修改本插件代码前。

## 功能一句话

**分「全局 / 项目」查看全部 skill（名称 / 描述 / 来源 / 状态），在设置页一键启用/禁用——禁用的 skill 不再注入该项目会话（模型不可见、不可加载），配置随仓库版本化。**

## 解决的问题

DSH 的 skill 机制自动发现并注入所有 skill（项目 `.dsh/skills`、`.agents/skills`、用户级 `~/.dsh/skills`、`customSkillDirs` 等），模型侧统一以 `<available_skills>` 呈现，但用户侧有两个空白：

1. **看不到 skill 全貌**：有哪些 skill、来自全局还是项目、是否启用，没有管理界面可查；
2. **无法按项目控制**：skill 全部自动注入，项目之间无法隔离——项目 A 不需要的 skill 也会注入，浪费 token 且可能干扰。

本插件补上这两个空白（issue #23）。

## 工作机制

```
设置 → 插件 → Skill 管理（官方 slots 扩展点，纯官方依赖）
        ↓ GET /skill-manager/api/list?cwd=…
ctx.skills.list({ cwd }) 合并目录 + 全局/项目配置
        ↓ PUT /skill-manager/api/config（保存 + control.invalidate()）
<项目根>/.dsh/skills.enabled.json（项目配置，随仓库版本化）
$DSH_HOME/skills.enabled.json（全局配置）
        ↓ 禁用生效
ctx.skills 注册的 rank-0 占位 provider（全局层）：
  被禁用的 skill 名在合并目录中被「已禁用」占位覆盖
  （filesystem rank 100–500、runtime 250 均低于 rank 0 优先级）
  → 模型侧不可见；get() 返回 undefined（不可加载）
```

## 业务场景映射

| 业务关键词 | 入口与方法（源码路径） |
|-----------|----------------------|
| 禁用占位 provider / 合并禁用名单 | `plugins/dsh-skill-manager/lib/provider.js`（server）— `createDisablerProvider()` / `disabledNamesOf()` |
| 全局/项目配置读写 | `plugins/dsh-skill-manager/lib/config.js` — `globalConfigFile()` / `projectConfigFileOf()` / `findProjectRoot()`（`.git` 向上查找） |
| API 路由（list / config） | `plugins/dsh-skill-manager/lib/api-route.js` — `GET /skill-manager/api/list`、`PUT /skill-manager/api/config` |
| 信任围栏 | `plugins/dsh-skill-manager/lib/fence.js` — 回环/可信 host 校验 |
| 设置页面板（全局/项目区块 + 开关） | `plugins/dsh-skill-manager/lib/client.js`（client）— `ctx.slots.inject('settings.plugins.tab', …)` |

## 安装与使用

1. 安装插件：

   ```bash
   dsh plugin --profile web add dsh-skill-manager
   ```

2. 打开 DSH Web 设置 → 插件 → **Skill 管理**；
3. 「全局」区块切换任意 skill 的全局启用/禁用；「项目」区块输入项目根路径后加载，即可编辑该项目级配置（含在项目内禁用全局 skill）；
4. 修改即时生效（catalog 失效重算），无需重启。

## 边界与限制

- 禁用是**按名字**生效：同名的任何来源 skill（项目/用户/内置）都会被占位覆盖；
- 配置只维护 `disabled` 名单（`enabled` 白名单模式预留，暂未实现）；
- 项目配置以 `cwd` 向上找 `.git` 定位项目根；无 `.git` 时退回 `cwd` 自身；
- 面板为可选增强：`ctx.get('slots')` 缺省时静默跳过注册，server 端禁用能力不受影响（与「纯官方依赖」原则一致，不依赖 dsh-better-sidebar）。

→ [需求清单](需求清单.md)

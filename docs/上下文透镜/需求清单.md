# dsh-my-context 需求清单

> 维护规则：任何功能开发或修改必须对照本清单做回归检查；新增功能必须在本清单补充条目并添加测试覆盖。

## 需求条目

| #   | 需求                    | 说明                                                                                                                                                                                                                                                                                   | 验收标准                                                                                                                   | 测试覆盖                                                                                                                                                   |
| --- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | 请求头统计              | Server 端监听 `session/event` 的 `request/header`：估算 system prompt 与 tools 的 token（与 dsh token-meter 一致的固定密度：~4 字符 ≈ 1 token + 4/block + 4/role），记录模型/提供方路由                                                                                                | 请求头更新后 system/tools 构成 > 0；模型/提供方被记录；无 header 对象的事件忽略                                            | `test/host-events.mjs`（request/header 统计）、`test/features/context.feature`（场景 1）                                                                   |
| R2  | 请求 token 计量         | Server 端监听 `assistant/message`：记录真实 usage（disjoint 计数 inputTokens/outputTokens/cacheReadTokens/cacheWriteTokens/reasoningTokens），每次请求快照构成（system/tools/user/inject/assistant/tool）进请求记录（prompt = input + cacheRead + cacheWrite）                         | 请求记录含真实 usage 与构成快照；空 content 的占位消息不重复计构成但记录 usage；无 usage 的 assistant/message 只计构成     | `test/host-events.mjs`（assistant/message 记录请求、空消息）、`test/host-store.mjs`（recordRequest 累加与快照）、`test/features/context.feature`（场景 2） |
| R3  | 上下文构成分类          | Server 端监听 `user/message`（含注入来源判定：source.kind 非 'user' 或带 form → inject 分类）、`tool/result`（tool 分类）、`assistant/message`（assistant 分类），估算各消息 token 累加进构成                                                                                          | 注入消息计入 inject 而非 user；工具结果计入 tool；各分类互不串扰                                                           | `test/host-events.mjs`（注入分类、tool 构成）、`test/features/context.feature`（场景 3）                                                                   |
| R4  | 会话隔离 + 查询接口     | 统计按 sessionId 分桶（会话隔离）；`GET /context/api/session?sessionId=` 查询详情、`GET /context/api/sessions` 会话列表（按最后活动倒序）、`GET /context/api/status` 状态 + 预算配置                                                                                                   | 不同会话统计互不可见；会话列表按最后活动排序；缺 sessionId 400、未知会话 404                                               | `test/host-store.mjs`（会话隔离、sessions 排序）、`test/host-smoke.mjs`（400/404）、`test/features/context.feature`（场景 4）                              |
| R5  | 持久化 + 重启恢复       | 统计持久化到 `$DSH_HOME/context/context.json`（防抖 500ms + 原子写 tmp+rename + teardown flush）；启动时异步加载（加载完成前变更缓冲不丢）                                                                                                                                             | 重启后统计完整恢复；加载完成前记录不丢失；坏文件/非法结构回退空状态不崩溃                                                  | `test/host-store.mjs`（重启恢复、坏文件）、`test/features/context.feature`（场景 5）                                                                       |
| R6  | 存储上限防膨胀          | 每会话请求记录上限 500（FIFO 淘汰）、告警上限 50（FIFO 淘汰）                                                                                                                                                                                                                          | 请求/告警数不超过上限（最旧被淘汰）；上限内不误淘汰                                                                        | `test/host-store.mjs`（FIFO 上限）                                                                                                                         |
| R7  | 预算控制                | 预算配置：每轮（perTurn）/每会话（perSession）token 上限（0=不限制）+ 模式（warn=超限提醒 / deny=超限拦截）；`agent/pre-step` waterfall 检查（total = input + output + cacheRead + cacheWrite），超限时 warn 记录告警透传 next()、deny 返回 `{ kind: 'reject' }` 结束本轮              | 每轮/每会话超限判定正确（每轮优先）；warn 不拦截只告警；deny 拦截且记录 blocked 告警；未超限透传；无统计会话/无 agent 透传 | `test/host-budget.mjs`（checkBudget 纯函数）、`test/host-events.mjs`（warn/deny/透传）、`test/features/context.feature`（场景 6-7）                        |
| R8  | 预算告警 + 冷却         | 超限告警记录（scope/limit/used/mode/blocked），同一会话同一 scope 60s 冷却防刷屏                                                                                                                                                                                                       | 告警记录正确；冷却期内不重复记录；`GET /context/api/alerts` 最新在前                                                       | `test/host-events.mjs`（告警冷却）、`test/host-mutation.mjs`（alerts 接口）                                                                                |
| R9  | 预算配置动态更新        | `POST /context/api/budget`（body { perTurn, perSession, mode }）动态更新预算配置（非法值回退默认）                                                                                                                                                                                     | 更新后 `GET /context/api/status` 返回新配置；非法值回退默认                                                                | `test/host-mutation.mjs`（budget 更新/非法值）、`test/features/context.feature`（场景 8）                                                                  |
| R10 | 上下文透镜面板          | Client 端侧边栏页签 `dsh-my-context:context`（dsh-better-sidebar 服务）：会话选择 + 概览卡片（累计 token/缓存命中率/模型）+ 构成条（system/tools/user/inject/assistant/tool 占比）+ 请求记录列表 + 预算设置（每轮/每会话上限 + 模式 + 保存）+ 预算告警列表；可见时 5s 轮询、隐藏时暂停 | 页签注册成功；面板展示当前会话统计；预算保存生效；隐藏时停止轮询                                                           | 手动验证（GUI）；client 语法由 CI `node --check` 覆盖；`test/features/context.feature`（数据侧）                                                           |
| R11 | 安全：loopback 信任围栏 | 所有 `/context/api` 请求先做 Host-header 信任围栏（loopback / trustedHosts / 同源校验，与 /api 网关一致契约）                                                                                                                                                                          | 非 loopback host 403；cross-site 403；本机 loopback 放行                                                                   | `test/host-smoke.mjs`（fence 403）、`test/host-mutation.mjs`（fence 变体：origin 匹配/localhost/trustedHosts/畸形 host）                                   |

## 回归检查（每次改动后）

- [ ] `npm test`（vitest + cucumber）通过
- [ ] 功能 R1-R3：真实对话中请求头/消息/工具结果被统计（`GET /context/api/session` 可查）
- [ ] 功能 R5：重启 DSH 后统计仍可查询
- [ ] 功能 R4：切换会话时面板只显示当前会话统计
- [ ] 功能 R7-R9：设置预算后超限提醒/拦截生效；告警出现在面板
- [ ] 功能 R10：侧边栏「上下文透镜」页签可见；概览/构成/请求/预算展示正常
- [ ] 无副作用：插件禁用/卸载后无残留样式、无残留轮询、无残留路由（`ctx.effect` 持有 disposer）

## 易碎点

- **`llm/request` 事件不存在**（DSH 核心 dsh-llm / dsh-agent-loop / dsh-session 均无此事件）：token 计量走 `session/event` 的 `assistant/message`（data.usage 为 disjoint 计数：inputTokens 已扣除 cacheReadTokens），prompt 还原 = input + cacheRead + cacheWrite。
- **`agent/pre-step` waterfall 契约**：监听器必须先 `await next()` 拿到下游决策再决定是否覆盖；warn 模式一律原样返回下游决策；deny 拦截返回 `{ kind: 'reject' }`（PreStepDecision 类型，reject 不带 reason 字段）。绝不吞掉 next()。
- **持久化竞态**：store 异步加载期间的事件必须缓冲；卸载（dispose）时未就绪缓冲要回放；加载完成时不能覆盖本进程已产生的事件（mergeCurrent）。
- **构成语义**：system/tools 构成 = 当前请求头估算（覆盖式，每次请求都携带）；user/inject/assistant/tool 构成 = 会话消息累积（累加式）。
- **告警刷屏**：同一会话同一 scope 60s 冷却，防止每步超限时告警爆炸。
- **存储膨胀**：请求记录每会话 500 条、告警 50 条 FIFO 淘汰。

# dsh-my-observability 需求清单

> 维护规则：任何功能开发或修改必须对照本清单做回归检查；新增功能必须在本清单补充条目并添加测试覆盖。

## 需求条目

| #   | 需求                            | 说明                                                                                                                                                                                                                                                                                                         | 验收标准                                                                                                                               | 测试覆盖                                                                                                                                                   |
| --- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | agent 状态审计                  | Server 端监听 `agent/status`，记录 `agent_status` 事件（状态 + 顶层/子代理标记，白名单化判定：`origin === 'subagent'` / `delegationDepth > 0` / 运行时 `options.subagentDepth > 0` 任一命中即子代理，header 缺失标记 unknown）                                                                               | 状态变化被记录且带 `agentType` 标记；无 session/header 的 agent 不记录或标记 unknown；事件含自增 id 与时间戳                           | `test/host-audit.mjs`（状态记录、子代理标记、null agent 忽略）、`test/features/observability.feature`（场景 1）                                            |
| 2   | 模型流审计                      | Server 端监听 `llm/stream`（waterfall），**同步**包装流透传全部 chunk，记录 `llm_stream` 开始/结束/错误事件（chunk 数、字符数、耗时、错误消息截断）                                                                                                                                                          | 流开始与结束各记录一条事件；chunk 全部透传（消费方无感知）；流错误记录 error 事件并向上传播；无 sessionId 时不记录且原样返回 next() 流 | `test/host-audit.mjs`（透传、统计、错误传播、无 sessionId）、`test/features/observability.feature`（场景 2）                                               |
| 3   | 工具调用审计                    | Server 端监听 `tools/pre-execute`（→ `tool_call`，含工具名 + 参数键列表 + 主要文本参数截断摘要）与 `tools/execute`（→ `tool_result`，含 ok/失败 + 耗时）；waterfall 一律透传 `next()`                                                                                                                        | 工具调用与结果各记录一条事件；`next()` 被调用、工具流程不受影响；无 agent 时不记录但 next 照常                                         | `test/host-audit.mjs`（tool_call/tool_result、next 透传、参数摘要截断、null args）、`test/features/observability.feature`（场景 3）                        |
| 4   | 审计持久化 + 重启恢复           | 审计事件持久化到 `$DSH_HOME/observability/audit.json`（防抖 500ms + 原子写 tmp+rename + teardown flush）；启动时异步加载（加载完成前事件缓冲不丢）                                                                                                                                                           | 重启后事件完整恢复；加载完成前记录的事件不丢失；坏文件/非法结构回退空状态不崩溃                                                        | `test/host-audit.mjs`（重启恢复、缓冲）、`test/host-mutation.mjs`（坏文件、非法事件过滤）、`test/features/observability.feature`（场景 5）                 |
| 5   | 会话隔离 + 查询接口             | 事件按 sessionId 分桶（会话隔离）；`GET /observability/api/events?sessionId&type&limit` 查询（正序时间轴、type 过滤、limit 取尾部）、`GET /observability/api/sessions` 会话列表（按最后活动倒序）、`GET /observability/api/status` 审计统计                                                                  | 不同会话事件互不可见；查询支持 type 过滤与 limit；会话列表按最后活动排序                                                               | `test/host-audit.mjs`（会话隔离、过滤、limit、sessions 排序）、`test/features/observability.feature`（场景 4、6）                                          |
| 6   | 审计上限防膨胀                  | 每会话事件上限 2000（FIFO 淘汰）；全局上限 20000（超限按最早活动会话整桶轮转淘汰）                                                                                                                                                                                                                           | 每会话事件数不超过 2000（最旧被淘汰）；全局事件数不超过 20000；上限内不误淘汰                                                          | `test/host-audit.mjs`（每会话上限 FIFO）、`test/host-mutation.mjs`（全局上限不误淘汰）                                                                     |
| 7   | 轨迹回放面板                    | Client 端侧边栏页签 `dsh-my-observability:replay`（dsh-better-sidebar 服务）：会话选择 + 类型过滤（全部/状态/模型流/工具）+ 时间轴事件列表（徽标 + 时间 + 摘要）；可见时 5s 轮询、隐藏时暂停                                                                                                                 | 页签注册成功；时间轴展示当前会话事件；会话切换/类型过滤生效；隐藏时停止轮询                                                            | 手动验证（GUI）；client 语法由 CI `node --check` 覆盖；`test/features/observability.feature`（数据侧）                                                     |
| 8   | 结构化 Git：类型化提交          | `POST /observability/api/git/commit`：Conventional Commits 消息（`<type>(<scope>): <description>` + 可选 body），type 枚举 feat/fix/docs/style/refactor/test/chore，scope 格式 `[a-z0-9-]+`，description 必填；`git add -A` + `git commit -m`（execFile 不经 shell）                                         | 合法请求提交成功并返回 hash + 消息；非法 type/空描述/非法 scope 400；非 git 仓库 400；提交消息格式正确                                 | `test/host-git.mjs`（formatCommitMessage/parseCommitRequest 纯函数、真实仓库提交）、`test/features/git.feature`（5 场景）                                  |
| 9   | 结构化 Git：状态/差异查询       | `GET /observability/api/git/status?repo=`（分支 + 变更清单 + staged/unstaged 计数）、`GET /observability/api/git/diff?repo=&staged=`（差异文本）                                                                                                                                                             | 状态解析正确（含未跟踪文件）；diff 区分工作区/暂存区；非仓库/缺 repo 400                                                               | `test/host-git.mjs`（status/diff 真实仓库）、`test/host-mutation.mjs`（API 层路由）                                                                        |
| 10  | 增量 diff 审查：规则引擎        | `POST /observability/api/review`：解析增量 diff（unified diff），规则引擎检查：debug-statement（warning）/secret-leak（error）/conflict-marker（error）/todo-marker（info）/trailing-space（info）/large-diff（warning，>300 行）/binary-file（warning）/no-test-change（info）；返回分级问题列表 + 变更统计 | 各规则命中/不命中正确；问题带 severity/rule/file/line；干净 diff 无 error/warning；非仓库 400                                          | `test/host-review.mjs`（parseDiff、各规则、分级）、`test/features/review.feature`（场景 1-4）                                                              |
| 11  | 增量 diff 审查：AI 增强（可选） | 配置 `aiReview`（默认开）且 agents 服务可用时，创建独立审查 agent 阅读 diff 输出 JSON 结论（verdict/summary/topIssues）；超时/失败/解析失败降级为 `{ failed, note }`，规则引擎结果不受影响                                                                                                                   | AI 结论合并到报告；agents 不可用/超时/解析失败时降级且规则报告完整；`aiReview: false` 配置或请求显式关闭时不运行                       | `test/host-review.mjs`（AI 成功/失败/不可用/解析失败/fence 包裹/配置关闭）、`test/host-mutation.mjs`（超时降级）、`test/features/review.feature`（场景 5） |
| 12  | 安全：loopback 信任围栏         | 所有 `/observability/api` 请求先做 Host-header 信任围栏（loopback / trustedHosts / 同源校验，与 /api 网关一致契约）                                                                                                                                                                                          | 非 loopback host 403；cross-site 403；本机 loopback 放行                                                                               | `test/host-audit.mjs`（fence 403）、`test/host-mutation.mjs`（fence 变体：origin 匹配/localhost/trustedHosts/畸形 host）                                   |

| 13  | 审计日志搜索/过滤               | Client 端轨迹回放面板新增：关键词搜索（匹配工具名/参数摘要/错误信息）+ 组合过滤（类型 + 时间范围 + 成功/失败）+ 命中关键词高亮。逻辑为纯函数（`lib/audit-view.js`），在已加载数据（≤2000 条）内过滤                                                                     | 可按关键词搜索工具名/参数/错误；组合过滤（类型/时间范围/成功失败）生效；命中关键词高亮；无匹配有提示                                            | `test/host-audit-view.mjs`（matchesKeyword / applyAuditFilter / highlightSegments / searchableText）                                           |
| 14  | 审计日志导出                   | 导出当前会话或全部会话审计日志：JSON（完整数据）或 CSV（摘要列：时间/类型/工具/结果），Blob + `a[download]` 下载；范围经独立下拉选择；全部会话走 `GET /observability/api/events?sessionId=*`（各会话合并）      | 可导出 JSON（含完整字段）与 CSV（摘要列表头正确）；导出范围可选（当前会话/全部会话）；CSV 转义正确                                   | `test/host-audit-view.mjs`（auditToJson / auditToCsv / csvCell 断言）、`test/host-audit.mjs`（sessionId='*' 全部会话合并）                        |
| 15  | 工具调用统计（可选）           | 面板统计视图（可折叠）：工具调用频次 Top N + 失败率，基于当前过滤后的事件聚合                                                                                                                              | Top N 统计与失败率正确；无数据时占位提示                                                                                               | `test/host-audit-view.mjs`（computeToolStats）                                                                                             |

## 回归检查（每次改动后）

- [ ] `npm test`（vitest + cucumber）通过
- [ ] 功能 1-3：真实对话中 agent 状态/模型流/工具调用被记录（`GET /observability/api/events` 可查）
- [ ] 功能 4：重启 DSH 后事件仍可查询
- [ ] 功能 5：切换会话时面板只显示当前会话事件
- [ ] 功能 7：侧边栏「轨迹回放」页签可见；时间轴展示事件；类型过滤生效
- [ ] 功能 7-扩展（#89）：面板关键词搜索/组合过滤生效；命中关键词高亮；JSON/CSV 导出可下载且范围可选（当前会话/全部会话）；工具统计展示
- [ ] 功能 8-9：Git 面板可查看仓库状态/diff；类型化提交成功且提交消息格式正确
- [ ] 功能 10-11：提交前审查返回问题列表；AI 审查不可用时降级不报错
- [ ] 无副作用：插件禁用/卸载后无残留样式、无残留轮询、无残留路由（`ctx.effect` 持有 disposer）

## 易碎点

- **llm/stream 必须同步包装**：cordis waterfall 不 await listener 返回值，`next()` 同步返回流；async listener 会让消费方（vision-toolkit 等 `yield*` 委托）拿到 Promise 而崩溃。包装流必须透传全部 chunk，只做统计。
- **waterfall 透传**：`tools/pre-execute` / `tools/execute` 监听器必须调用 `next()`，漏掉会导致工具流程挂死。
- **持久化竞态**：store 异步加载期间的事件必须缓冲；卸载（dispose）时未就绪缓冲要回放；加载完成时不能覆盖本进程已产生的事件（mergeCurrent）。
- **审计膨胀**：参数摘要必须截断（键列表 ≤10、文本 ≤200 字符）；每会话/全局上限必须生效。
- **git 安全**：全部经 `execFile`（不经 shell），提交消息由服务端生成，不拼接用户输入。
- **AI 是增强不是门禁**：AI 审查任何失败（服务不可用/超时/解析失败）都降级为 failed 标记，规则引擎结果不受影响。
